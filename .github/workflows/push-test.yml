name: Push Test

on:
  push:
    branches:
      - "release-v[0-9]+\.[0-9]+\.[0-9]+"

jobs:
  update-log:
    name: "Draft a new release"
    runs-on: ubuntu-latest
    steps:
      - name: Update log
        run: echo "Updating change log..."
